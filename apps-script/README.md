# apps-script/ — Google Apps Script 備份通道原始碼

此資料夾是 Google Sheet 備份通道的 Apps Script 原始碼；實際執行在 Google 端（各倉庫的部署 URL 寫在 `index.html` 的 `GS_WEBHOOKS`）。
`index.html` admin 面板的「Backup ke Google Sheet」按鈕仍在使用此通道。
